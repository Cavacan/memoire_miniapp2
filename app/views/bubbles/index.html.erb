<p style="color: green"><%= notice %></p>

<h1>思い出バブル</h1>

<div id="bubbles">
  <% if @bubbles.any? %>
    <% @bubbles.each do |bubble| %>
      <%= render bubble %>
      <p>
        <%= link_to "思い出を見る", bubble %>
      </p>
    <% end %>
  <% else %>
    <div class="no-bubble">
      <p>これからたくさん思い出を作っていきましょう。</p>
    </div>
  <% end %>
</div>

<%= link_to "New bubble", new_bubble_path %>
